import { AccountVerificationForm } from "@/components/form/AccountVerificationForm";

export default function AccountVerificationPage() {
      return (
            <main className="w-full flex items-center justify-center">
                  <AccountVerificationForm />
            </main>
      )
}